# Update for the meeting, 24 September 2026

## One-line summary

A small model reading Google AlphaEarth embeddings reproduces the ESA CCI biomass map to within CCI's own stated per-pixel uncertainty (RMSE ≈ 0.73 × CCI SD), in France (trained) and in Sweden (never trained on).

## Results

| | RMSE (Mg/ha) | R² | vs CCI SD |
|---|---|---|---|
| Ridge, linear per pixel, France test | 31.7 | 0.73 | |
| PointWise MLP, France test | 27.7 | 0.80 | |
| SmallCNN, France test | 26.3 | 0.82 | 0.73 |
| SmallCNN, Sweden (transfer, 100 tiles) | 27.0 | 0.65 | 0.72 |

- Linear already gets R² 0.73: the information is in the embedding.
- Stable across two independent random samples of France (26.1 vs 26.5 RMSE).
- **Seed repeats (3 CNN runs, same test tiles, different weight init), done overnight:**

  | | RMSE | R² | bias | RMSE / CCI SD |
  |---|---|---|---|---|
  | France test | 26.7 ± 0.6 | 0.81 ± 0.01 | +0.8 ± 1.3 | 0.74 ± 0.02 |
  | Sweden | 24.4 ± 3.5 | 0.71 ± 0.08 | +6.1 ± 6.5 | 0.65 ± 0.09 |

  In-domain is tight. Out-of-domain the spread is large: two seeds over-predict Sweden by ~10 Mg/ha, one is unbiased (RMSE 20.4, R² 0.80). Transfer quality depends on the particular solution the training found, so any transfer claim needs several seeds.

- **Ensemble of the three (predictions averaged), done 24 Sep morning:**

  | | RMSE | R² | bias | RMSE / CCI SD | within 1 SD |
  |---|---|---|---|---|---|
  | France test | 26.1 | 0.82 | +0.8 | 0.73 | 67 % |
  | Sweden | **21.9** | **0.77** | +6.1 | **0.58** | 80 % |

  Averaging cancels the random part of the transfer error (Sweden RMSE 24.4 → 21.9, R² 0.71 → 0.77) but not the bias, which is simply the members' mean (+6). In-domain nothing changes, as expected. The ensemble is now the best Sweden result and the recommended model for transfer.
- Saturation above ~150 Mg/ha, inherited from the reference.

## Done this week (22–23 Sep)

1. Evaluation script (`src/evaluate.py`): RMSE/MAE/R²/bias, per-tile spread, scatter, ridge baseline, transfer mode, CCI-SD comparison.
2. Found a download bug (single AlphaEarth image instead of a mosaic → 7 % of pixels empty, 16 of 100 tiles). Fixed; re-downloaded France and Sweden.
3. CCI's per-pixel SD band now saved with every tile; back-filled for existing data.
4. Input normalisation + target scaling (the per-pixel model did not converge without it); `train_meta.json` next to each checkpoint so evaluation is reproducible.
5. Separate `--init_seed` so repeat runs keep identical test tiles.
6. ESA Climate Toolbox path: standalone scripts (`targets_ect.py`, `build_ect.py`) in their own conda env; fetches targets for any CCI ECV and regrids onto the tile grid. Verified end to end. Found a toolbox bug (ODP store fails on biomass v5–v7), so the toolbox path currently gives v4.
7. Repo: old folders archived, data under `data_gee/`, scripts renamed by source (`build_gee`, `build_ect`), setup guide merged into README, all committed and pushed to the fork.

## Literature, started

`docs/lit_review.md`. Key comparator: Sialelli et al. 2026 (arXiv 2608.04792) benchmarked frozen AlphaEarth + small head against GEDI (RMSE ~52 Mg/ha) and found it on par with CCI against plots (34.3 vs 34.8). The AlphaEarth paper itself has no biomass task. European maps vs plots run 78–80 Mg/ha (Avitabile & Camia 2018). Two papers still to get via the library (Araza 2022; the Andean spectral-indices counter-example).

## Next

1. Seed repeats and ensemble: done (above). The remaining +6 Mg/ha Sweden bias is systematic; a next step would be to check whether it is a biomass-range effect (Sweden has fewer near-zero pixels) or a true domain shift, e.g. by binning bias by CCI value.
2. Literature: read Sialelli 2026 properly, fetch the paywalled two.
3. Second ECV via the toolbox: needs a fine-resolution product (land cover, burned area). Coarse ECVs need a different tile design. **Ask the group which.**
4. GeoTessera as a second embedding through the same pipeline.
5. Merge `data-fixes` into `main`.
